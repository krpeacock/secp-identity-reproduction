# Secp256k1 Identity Bug Reproduction

The code in this repository does not reproduce the issue described in [this ticket](https://github.com/dfinity/agent-js/issues/679).

To run, first install dependencies:

```bash
npm install
```

Then run the test:

```bash

npm run start

```
